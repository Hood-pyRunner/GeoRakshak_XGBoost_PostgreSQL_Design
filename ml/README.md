# GeoRakshak Machine Learning

This directory will contain reproducible feature pipelines, XGBoost training, evaluation, model cards, and optional `pgvector` experiments.

The model must use versioned structured feature vectors generated from PostgreSQL/PostGIS. Do not train on unverified or unmonitored labels.
